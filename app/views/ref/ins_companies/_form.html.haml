= simple_form_for @ref_ins_company do |f|
  -if @ref_ins_company.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ref_ins_company.errors.count, "error")} prohibited this ref_ins_company from being saved:"
      %ul
        - @ref_ins_company.errors.full_messages.each do |msg|
          %li= msg
  =f.input :name
  =f.submit "Сохранить"
