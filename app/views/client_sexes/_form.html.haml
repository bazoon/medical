= form_for @client_sex do |f|
  -if @client_sex.errors.any?
    #error_explanation
      %h2= "#{pluralize(@client_sex.errors.count, "error")} prohibited this client_sex from being saved:"
      %ul
        - @client_sex.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :sex
    = f.text_field :sex
  .actions
    = f.submit 'Save'
